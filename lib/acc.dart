import 'package:flutter/material.dart';

class Acc extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return new Scaffold(
      body: Center(
        child: Text('c est l\'accueil'),
      ),
    );
  }
}